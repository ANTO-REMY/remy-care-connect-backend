Place the local Nairobi polygon files for one-time facility enrichment here:

- `nairobi_wards.geojson`
- `nairobi_subcounties.geojson`

Both files should be in `EPSG:4326` so facility longitude/latitude points can be matched directly without reprojection at runtime.
